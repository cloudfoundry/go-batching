language: go

go:
  - 1.8.x
  - 1.9.x
  - master

install:
- go get -d -v -t code.cloudfoundry.org/go-batching/...
- go generate code.cloudfoundry.org/go-batching/...

script: go test code.cloudfoundry.org/go-batching/...

matrix:
  allow_failures:
  - go: master
